# When a Task Is Completed

- Run `make check` (format + lint + typecheck + test) when changes are significant.
- Ensure new behavior has tests and naming follows pytest conventions.
- Maintain 80%+ coverage where applicable.
- Update docs/README if user-facing behavior or CLI options change.
- Use commit message format `type: description` and keep scope focused.