[build-system]
requires = ["setuptools>=45"]
build-backend = "setuptools.build_meta"

[project]
name = "kardscm"
version = "0.9.0"
description = "KARDS Manager for player collection and decks with multi-format export"
readme = "README.md"
requires-python = ">=3.12"
license = { text = "MIT" }
authors = [
    { name = "Alexander Burchenko", email = "aabur@mail.ru" }
]
keywords = ["kards", "card-game", "collection", "deck", "scraper"]
classifiers = [
    "Development Status :: 4 - Beta",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3.12",
]
dependencies = [
    "openpyxl>=3.1.0",
    "httpx>=0.27.0",
    "typer>=0.9.0",
    "fastapi>=0.136.1",
    "jinja2>=3.1",
    "uvicorn[standard]>=0.46.0",
    "python-multipart>=0.0.31",
]

[project.urls]
Homepage = "https://github.com/AABur/kardscm"
Repository = "https://github.com/AABur/kardscm"
Issues = "https://github.com/AABur/kardscm/issues"

[project.scripts]
kardscm = "kardscm.cli:run"

[project.optional-dependencies]
dev = [
    "pytest>=9.0.3",
    "pytest-cov>=4.1.0",
    "pytest-asyncio>=0.23.0",
    "ruff>=0.1.0",
    "mypy>=1.8.0",
]

[tool.setuptools.packages.find]
where = ["."]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP"]
ignore = []

[tool.ruff.lint.isort]
known-first-party = ["kardscm"]

[tool.mypy]
python_version = "3.12"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v --strict-markers --cov=kardscm --cov-report=term-missing"

[dependency-groups]
dev = [
    "pytest-cov>=7.0.0",
]
